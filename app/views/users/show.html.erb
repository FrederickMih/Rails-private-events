<%= link_to 'New Event', new_event_path %> <br>
<%= link_to 'New User', new_user_path %>
<br>
<br>

<h1>User <%= @user.id %> <%= @user.username %></h1> 


<% if @user.events.empty? %>
  <h3> <%= @user.username %> Didn't make any event </h3>
<% else %>
  Number of events mades: <%=  @user.events.size %>
  <br><br>
  <% @all_events = @user.events %>
  <% @all_events.each do |e| %>
    <%= e.description %>
  <% end %>
<% end %>

<h3> Upcoming Events <h3>
 <% @user.attended_events.upcoming.each do |event| %>
      <tr>
        <td><%= event.name %></td>
        <td><%= event.description %></td>
        <td><%= event.date %></td>
      </tr>
  <% end %>

<h3> Past Events <h3>
 <% @user.attended_events.past.each do |event| %>
      <tr>
        <td><%= event.name %></td>
        <td><%= event.description %></td>
        <td><%= event.date %></td>
      </tr>
  <% end %>




<% %>
<% %>
<% %>
<% %>
<% %>

