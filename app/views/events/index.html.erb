<h1>All events</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @event.each do |event| %>
      <tr>
        <td><%= event.description %></td>
      </tr>
    <% end %>
        
        
  </tbody>
</table>

<br>

<%= link_to 'New event', new_event_path %>